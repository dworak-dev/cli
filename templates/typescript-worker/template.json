{
  "name": "typescript-worker",
  "description": "This template is used to create a worker project for node with typescript. It includes a basic test setup with jest, eslint and much more.",
  "commands": [
    {
      "name": "yarn",
      "description": "Install dependencies"
    },
    {
      "name": "yarn start:dev",
      "description": "Start the application in development mode"
    },
    {
      "name": "yarn build",
      "description": "Build the application"
    }
  ]
}
